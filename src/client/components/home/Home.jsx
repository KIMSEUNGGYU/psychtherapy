import React, { Fragment } from "react";

import { Main, Services, Feature } from "client/components";

const Home = () => {
    return (
        <Fragment>
            <Main />
            <Services />
            <Feature />
        </Fragment>
    );
};

export default Home;
