import { connect } from "react-redux";
import { CounselingPayment } from "client/components";

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(CounselingPayment);
